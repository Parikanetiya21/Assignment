Web Designing Assignment
Term-1
Module (HTML)-1

1. Are the HTML tags and elements the same thing?
Ans- No, HTML tags and elements are not the same thing. HTML tags are utilized to hold the HTML element though HTML element are utilized to holds the substance. HTML label begins with < and closes with > and whatever composed inside <> are elements. For instance: <p></p>. Here p is the HTML tag and when it is in the middle between <> for example <p></p> then it is called HTML element.

2.What are tags and attributes in HTML?
Ans- HTML tags are utilized to hold the HTML element. All HTML elements can have attributes in them. Characterizing the personality of a HTML element is utilized. It generally positioned in the initial tag of an element. It for the most part gives extra styling (attribute) to the element. For instance- <img src="mydog.jpg" alt="A pic of my dog.">. In this, the image source (src) and the alt text (alt) are attributes of the <img> tag.

3.What are void elements in HTML?
Ans- Void elements doesn't have finishing tags and can have attributes yet contain no sort of content. Example of such elements are <br>, <hr>, <img>, <input>, <link>.

4.What are HTML Entities? 
Ans- A HTML entity is utilized to show undetectable characters and held characters that would somehow be deciphered as HTML code. It is a piece of text, or string, that starts with an ampersand (&) and closes with a semicolon (;).

5.What are different types of lists in HTML?
Ans- Types of lists in HTML are:

     a) Unordered list — used to group a set of related items in no particular order
     b) Ordered list — used to group a set of related items in a specific order
     c) Description list — used to display name/value pairs such as terms and definitions
     d) Nested list- A nested list is a list inside a list.

6.What is the ‘class’ attribute in HTML?
Ans- The class attribute determines at least one class names for an element.Class is represented by ‘.’ Symbol. 

7. What is the difference between the ‘id’ attribute and the ‘class’ attribute of HTML elements?
Ans- In Html for an element ID name begins with the "#" image followed by a special name doled out to it. Then again class appointed to an element has its name begins with "." followed by class name. Id is special in a page and can apply to all things considered one element. The class can be applied to different elements so it very well may be on numerous occasions on a single page.

8.What are the various formatting tags in HTML?
Ans- To change the designing of text, HTML offers an assortment of predefined elements. The text can be highlighted, made superscript or subscript, or given other text styles, such as bold, italic, or emphasised, using formatting.

9.How is Cell Padding different from Cell Spacing?
Ans- Cell padding fundamentally characterizes the space present between a table cell's line and the substance present in it. Cell spacing fundamentally characterizes the space present between individual nearby cells.

10. How can we club two or more rows or columns into a single row or column in an HTML table? 
Ans- We club at least two rows or columns into a single row or column by utilizing Colspan and Rowspan attributes. We can consolidate at least two table cells in a segment utilizing the colspan, while to combine at least two column cells, utilize the rowspan attribute.

11. What is the difference between a block-level element and an inline element? 
Ans- A block-level element generally begins another line, this element generally takes up the full width accessible where an inline element doesn't begin another line. This element just takes up as much width as the need should arise.

12. How to create a Hyperlink in HTML? 
Ans- To make a hyperlink in a HTML page, utilize the <a> and </a> tags, which are the tags used to characterize the connections. The <a> tag shows where the hyperlink begins and the </a> tag demonstrates where it closes. Anything text gets added inside these tags, will function as a hyperlink. Add the URL for the connection in the <a href=" ">.

13. What is the use of an iframe tag? 
Ans- The <iframe> tag determines an inline frame. An inline frame is utilized to insert one more record inside the ongoing HTML report. The 'src' attribute is utilized to determine the URL of the document that involves the iframe.

14. What is the use of a span tag? Explain with example? 
Ans- The <span> tag is an inline holder used to mark up a piece of a text, or a piece of a document. The <span> tag is similar as the <div> element, however <div> is a block-level element and <span> is an inline element.The span tag is utilized for the grouping of inline elements and this tag rolls out no visual improvement without anyone else.
    Eg: <body>
<p> Welcome to the <span> flow dance academy </span></p> 
     </body>

15. How to insert a picture into a background image of a web page? 
Ans- To set the background image of a webpage, utilize the CSS style. Under the CSS <style> tag, add the property background-image. The property sets a realistic, for example, jpg, png, svg, gif, and so on.

16. How are active links different from normal links?
Ans- Normal links will be links which are there on the page and have not been clicked at this point. Active links are those links, which have recently been clicked right then and there.

17. What are the different tags to separate sections of text? 
Ans- Three tags are utilized to separate the texts.
     i) <br> tag - Usually <br> tag is utilized to isolate the line of text. It breaks the ongoing line and passes the stream on to the following line.
     ii) <p> tag - The <p> tag contains the text as another paragraph.
     iii) <blockquote> tag - It is utilized to characterize an enormous cited segment. In the event that you have an enormous citation, put the whole text inside <blockquote></blockquote> tag.

18. What is SVG? 
Ans- The HTML SVG is an acronym which stands for Scalable Vector Graphics. It depict two-layered vector and blended vector/raster designs in XML. SVG is for the most part utilized for vector type outlines like pie diagrams, 2-Dimensional charts in a X,Y coordinate framework and so on. The <svg> element determines the foundation of a SVG section. You can enliven each element and each attribute in SVG records.

19. What is difference between HTML and XHTML? 
Ans-

20. What are logical and physical tags in HTML?
Ans-
