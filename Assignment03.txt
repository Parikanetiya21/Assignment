Web designing
Module (HTML5) – 3 
1.What are the new tags added in HTML5? 
Structural or Semantic Tags:
-<article>: It characterizes the free or independent content of a webpage.
-<aside>: It characterizes the content which give data about the primary content.
-<bdi>: It is utilized to disconnect the piece of text which may be arranged toward another path.
-<details>: It characterizes extra data which just apparent according to client interest.
-<dialog>:  It represents a dialog box or other interactive components.
-<figcaption>: It defines caption for the <figure> element.
-<figure>: It defines a self-contained content, and referenced as a single unit.
-<footer>: It represents the footer section of the webpage.
-<header>: It characterizes the basic or navigational content of the webpage.
-<main>: It indicates the principal content of the HTML document.
-<mark>: It address the text which is featured or set apart for reference or documentation purposes.
-<meter>: It represents a scalar value within a known range.
-<nav>: It represents the section which contains navigation links.
-<progress>: It characterizes an advancement bar which shows culminations progress of a task.
-<rp>: It defines alternative content for the browser which do not support ruby annotations.
-<rt>: It defines explanations and pronunciations of characters in ruby annotations.
-<ruby>: It defines ruby annotations (Specifically for Asian language).
-<section>: It characterizes a conventional segment inside a HTML record.
-<summary>: It defines summary or caption for a <details> element which can be clicked to change the state of <details> element.
-<time>:It defines data/time within an HTML document.
-<wbr>: It specifies a line break opportunity. (Where line break possible)
Graphics Tags:
-<canvas>: It allows drawing graphics and animations via scripting.
-<svg>: It is used to draw scalable vector graphics.
HTML5 Form Tags:
-<datalist>: It represent predefined list for input <option> element.
-<output>: It is utilized a compartment component to address the result of a computation or result of client activity.
HTML5 Media Tags
-<video>: It defines video content within HTML document.
-<source>: It defines multiple media resources for the media elements.
-<audio>: It defines sound content.
-<embed>: It defines a container for external files/application/media.
-<track>: It defines text tracks for <audio> and <video> files.

2.How to embed audio and video in a webpage? 
The <embed> component is utilized to embed multimedia content into a HTML document.
The following code fragment embeds audio files into a web page:
When you click the share button, a share panel will open displaying some more buttons. Now click on the Embed button, it will generate the HTML code to directly embed the video into the web pages. Just copy and paste that code into your HTML document where you want to display the video and you're all set.

3.Semantic element in HTML5? 
Article,aside,details,figcaption,figure,footer,header,main,mark,nav,section,summary and time is semantic elements in html5.

4.Canvas and SVG tags
Canvas tags:
-Canvas is raster based and made out of pixel.
-Canvas can be altered through script as it were.
-Canvas has unfortunate adaptability. Subsequently it isn't reasonable for imprinting on higher goal.
SVG Tags:
-SVG is vector based and composed of shapes.
-SVG can be modified through script and CSS.
-SVG has better scalability. So it can be printed with high quality at any resolution.




